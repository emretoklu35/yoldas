generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model User {
  id       Int    @id @default(autoincrement())
  email    String @unique
  password String
  role     String @default("user")

  resetToken       String? // 💡 geçici token alanı
  resetTokenExpiry DateTime? // 💡 token süresi (opsiyonel ama önerilir)
}
